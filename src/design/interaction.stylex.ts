import * as stylex from "@stylexjs/stylex";
export const group = stylex.defineMarker();
export const card = stylex.defineMarker();
export const lightbox = stylex.defineMarker();
export const tooltip = stylex.defineMarker();
